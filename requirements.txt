# Agent Contest Python Demo - Requirements
# Python 3.11.0+ required (not a pip package — install via system package manager)

# ============================================================
# Core Dependencies
# ============================================================
openpyxl>=3.1.0            # Excel (.xlsx) read/write
pandas>=2.0.0              # Data analysis (includes numpy)

# ============================================================
# Document Parsing
# ============================================================
python-docx>=1.1.0         # Word (.docx)
python-pptx>=0.6.23        # PowerPoint (.pptx)
pdfplumber>=0.11.0         # PDF text extraction
beautifulsoup4>=4.12.0     # HTML/XML parsing
Pillow>=10.0.0             # Image processing

# ============================================================
# System Dependencies (not pip-installable)
# ============================================================
# Java JDK — for code_execute Java support
# Node.js — for code_execute JavaScript support
