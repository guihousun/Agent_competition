{
  "name": "java_tax_solver",
  "description": "Deterministically solve broken Java personal income tax calculator contest tasks.",
  "entrypoint": "scripts/run.py",
  "timeout_seconds": 20,
  "input_schema": {
    "type": "object",
    "properties": {
      "source_path": {
        "type": "string",
        "description": "Path to the Java source file."
      },
      "question": {
        "type": "object",
        "description": "Question object or text-containing object with hidden cases.",
        "additionalProperties": true
      }
    },
    "required": ["source_path", "question"],
    "additionalProperties": false
  }
}
