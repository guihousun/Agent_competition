# Streaming And 600-Second Timeout Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make `stream=true` and a 600-second model timeout the reliable default required by the competition.

**Architecture:** Keep streaming normalization inside `ChatCompletionClient` so the main agent and sub-agents consume the same ordinary completion structure. Extend the SSE parser only for competition-relevant OpenAI-compatible events and keep environment overrides available.

**Tech Stack:** Python standard library, `urllib`, Server-Sent Events, `unittest`.

---

### Task 1: Default configuration regression tests

**Files:**
- Modify: `tests/test_p0_runtime_fixes.py`
- Modify: `source/runtime/env_config.py`

- [x] Change the timeout-default assertion from 300 to 600 seconds.
- [x] Add an assertion that stream defaults to true.
- [x] Add an assertion that explicit false and a custom timeout still override defaults.
- [x] Run the focused tests and confirm they fail before implementation.
- [x] Change the production defaults and rerun the focused tests.

### Task 2: SSE normalization regression tests

**Files:**
- Create: `tests/test_streaming_client.py`
- Modify: `source/runtime/openai_chat_client.py`

- [x] Test fragmented assistant content and reasoning.
- [x] Test fragmented tool-call name and arguments.
- [x] Test a usage-only tail chunk and `[DONE]`.
- [x] Test stream-level error payloads.
- [x] Test an SSE stream with no usable choice.
- [x] Test that the HTTP request sends `stream=true` and uses a 600-second timeout.
- [x] Run the focused tests and confirm the missing behavior fails.
- [x] Implement minimal SSE metadata, usage, error, and empty-stream handling.
- [x] Rerun the focused tests.

### Task 3: Documentation synchronization

**Files:**
- Modify: `.env.example`
- Modify: `README.md`
- Modify: `GUIDE.md`
- Modify: `DESIGN_CORE_ENHANCEMENTS.md`

- [x] Set documented defaults to 600 seconds and stream true.
- [x] Explain that explicit environment values can override defaults.
- [x] Remove conflicting timeout and stream examples.

### Task 4: Full verification

**Files:**
- Test: `tests/test_p0_runtime_fixes.py`
- Test: `tests/test_runtime_hardening.py`
- Test: `tests/test_streaming_client.py`

- [x] Run `python -m unittest discover -s tests -v`.
- [x] Run `python -m compileall -q source tests`.
- [x] Run `git diff --check`.
- [x] Run available official-case regressions and compare answers where references exist.
- [x] Commit and push the verified changes to the current GitHub PR branch.
